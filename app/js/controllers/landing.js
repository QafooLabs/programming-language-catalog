'use strict';

/**
 * Controller to be used for the initial landing page
 *
 * @param $scope
 * @constructor
 */
function LandingController($scope) {
}

// DI injection definition (Only needed if code is minified)
LandingController.$inject = [];